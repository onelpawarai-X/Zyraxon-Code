name: 'Telemetry'
on: pull_request
permissions: {}
jobs:
  check-metadata:
    name: 'Check metadata'
    runs-on: 'ubuntu-latest'

    steps:
      - uses: 'actions/checkout@v6'
        with:
          persist-credentials: false

      - uses: 'actions/setup-node@v7'
        with:
          node-version: 'lts/*'

      - name: 'Run zyraxoncode-telemetry-extractor'
        run: 'npx --package=@zyraxoncode/telemetry-extractor@1.14.0 --yes zyraxoncode-telemetry-extractor -s .'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
